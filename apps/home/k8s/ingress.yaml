apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: bear-detector-app
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`home.local`) && PathPrefix(`/api`)
      services:
        - name: api/bear-detector-app
          port: 3000